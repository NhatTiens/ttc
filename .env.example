# Work 04 — local development only. Copy to .env and replace all secrets.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/tuong_tac_pro?schema=public"
TEST_DATABASE_URL="postgresql://postgres:postgres@localhost:5432/tuong_tac_pro_test?schema=public"

# Generate with: openssl rand -base64 32
AUTH_SECRET="replace-with-at-least-32-random-bytes"
AUTH_TRUST_HOST="true"
# Optional when running behind a non-standard host/proxy.
# AUTH_URL="http://localhost:3000"

# Idempotent development seed account. Never use these values in production.
SEED_DEVELOPMENT_ACCOUNT="true"
SEED_DEVELOPMENT_EMAIL="minh@example.com"
SEED_DEVELOPMENT_PASSWORD="demo1234"

# Work 05 — development-only admin/secondary customer seed accounts.
# Never reuse these passwords in production. Override them in local .env if desired.
SEED_ADMIN_EMAIL="admin@example.com"
SEED_ADMIN_PASSWORD="Admin1234"
SEED_DEVELOPMENT_SECOND_EMAIL="lan@example.com"
SEED_DEVELOPMENT_SECOND_PASSWORD="demo1234"

# Optional admin runtime smoke target.
# WORK5_BASE_URL="http://localhost:3000"

# Work 06 - Provider Integration
PROVIDER_ROUTING_ENABLED=false
PROVIDER_WORKER_ID=
PROVIDER_WORKER_POLL_MS=1000
PROVIDER_JOB_BATCH_SIZE=10
PROVIDER_JOB_LOCK_TIMEOUT_MS=300000
PROVIDER_MAX_ATTEMPTS=6
PROVIDER_POLL_INITIAL_MS=60000
PROVIDER_POLL_MAX_MS=1200000

# Tương Tác Chéo API v2 (documented provider contract).
# Secrets are server-side only and must never use NEXT_PUBLIC_ prefixes.
TTC_API_BASE_URL="https://tuongtaccheo.com/api/v2"
TTC_API_KEY="ubfnatoa3ojrmyj5mysmmc5tbsmad1y7"
TTC_HTTP_TIMEOUT_MS=10000
# TTC service rates are reported in XU. Set the authoritative VND value of 1 XU
# before syncing services or enabling provider routing. Example only: 1000 means 1 XU = 1000 VND.
TTC_XU_TO_VND_RATE="0.05"
# TTC rate is treated as the cost for this quantity unit; API v2 follows the standard per-1000 model.
TTC_RATE_UNIT=1000

# Optional LIVE API smoke. Keep disabled unless intentionally sending a real provider order.
TTC_LIVE_TEST_ALLOW_ORDER=false
TTC_LIVE_TEST_SERVICE_ID=
TTC_LIVE_TEST_LINK=
TTC_LIVE_TEST_QUANTITY=
